interface:
  display_name: "CoTend Project Init"
  short_description: "Route and init AI workflow"
  default_prompt: "Use $cotend-project-init Auto Mode to inspect the project, classify fresh_init, legacy_update, partial_repair, current_resume, or blocked_human_decision, then load cotend-collaboration for governance. Preserve user files and active truth; coordinate Plan Tree, expanded understanding, decision and knowledge logs, review, risk, Code Context and Acceptance/Test Harnesses; report a compact initialization summary and readiness. Stop for conflicting product truth, destructive or public actions, and other human-only decisions; bare continue does not answer them."
