# Isolated Codex carrier fixture

This template is copied into a Git-ignored nested repository by
`scripts/verify_isolated_codex_carrier.py`. The harness then installs the
repository Skill source into `.agents/skills` and verifies Codex discovery.

The template contains no credentials, remote, backend, or product state.
